// GrapesJSEditor.tsx — Legacy stub (editor removed in Phase 4 rebuild)
// This file is kept to prevent import errors from any remaining references.

export default function GrapesJSEditor() {
    return <div style={{ padding: 40, textAlign: "center", color: "#999" }}>Editor has been upgraded. Use the block editor.</div>;
}
